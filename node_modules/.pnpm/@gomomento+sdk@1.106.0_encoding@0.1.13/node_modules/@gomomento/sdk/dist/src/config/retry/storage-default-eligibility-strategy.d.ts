import { MomentoLoggerFactory } from '../../';
import { EligibilityStrategy, EligibleForRetryProps } from './eligibility-strategy';
export declare class DefaultStorageEligibilityStrategy implements EligibilityStrategy {
    private readonly logger;
    constructor(loggerFactory: MomentoLoggerFactory);
    isEligibleForRetry(props: EligibleForRetryProps): boolean;
    private getRetryDispositionMetadata;
}
